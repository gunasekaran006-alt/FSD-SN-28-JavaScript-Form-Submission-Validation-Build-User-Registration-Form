# Simple JS Form Validation

A basic implementation of client-side form validation using Vanilla JavaScript.

## Features
- Prevents empty submissions.
- Uses **RegEx** for email format validation.
- Validates password length.
- Prevents default form refresh using `event.preventDefault()`.

## Setup
Just open `index.html` in any web browser to see the validation in action.